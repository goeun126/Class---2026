#python , ''
"변수선언"은 해당 변수를 리셋하는 단계 , 특정 데이터를 넣을경우 "변수 초기화"라고 한다 = 변수정의(definition /def)
​
a == b a와 b는 같다. => True or False / 진실 혹은 거짓
a != b a와 b는 같지않다. => True or False / 진실 혹은 거짓
a > b a가 b보다 크다. => True or False / 진실 혹은 거짓
a >= b a가 b보다 크거나 같다. => True or False / 진실 혹은 거짓
a < b a가 b보다 작다. => True or False / 진실 혹은 거짓
a <= b a가 b보다 작거나 같다. => True or False / 진실 혹은 거짓

slicing(슬라이싱) = 필요한 부분만 뽑아내는 것
print(f'{변수명[인덱스 필요한 시작 번호:끝나는 번호 +1]}')
               0은 생략이 가능함 [:끝나는 번호+1)
print(f' {변수명[len(변수명)-2:]}')
print(f' {변수명[:-1]}') => 리스트 뒤에서 뺀다
print(f' {변수명[-1:]}') => 앞에서 뺀다
print(f' {변수명[:]}')   => 전체출력
print(f' {변수명[::n]}') => 스텝 ::두개찍을경우
                            n칸식 건너뛰면서 이동하는 것
						   -n 일경우 뒤에서 앞으로 건너뛰면서 이동하는 것
sort = 리스트 정렬
reverse = 뒤집다 라는뜻 
          False면 오름차순(ASC)
          True면 내림차순(DESC)으로 정렬
변수명.reverse()
print(f'변수명:{변수명}')
		
# 오름차순(ASC)
변수명.sort(reverse=False)
print(f'변수명:{변수명}')

변수명.sort() -> 기본으로 오름차순이기때문에 함수를 넣지 않아도 ok
print(f'변수명:{변수명}')

# 내림차순(DESC)
변수명.sort(reverse=True)
print(f'변수명:{변수명}')

remove = 특정 아이템 삭제
변수명.remove('삭제할 리스트')
print(f'변수명:{변수명}')

del 와 pop 차이점
변수명 = nums.pop(3) < 삭제할 아이템 보관 가능 del은 불가
print(f'변수명:{변수명}')
pop = 리스트 삭제
변수명.pop() <- 인덱스 번호를 기재 안하면 마지막 삭제 / 특정위치 리스트 삭제
print(f'변수명:{변수명}')
del / 완전삭제
del 변수명[삭제할 인덱스 번호]

insert = 리스트 연결하기
변수명.extend(변수명)
print(f'변수명:{변수명}')

append = 변수명에 추가를 하는 방법
변수명.append('추가할 item')
print(f'변수명:{변수명}')

index = 할당된 번호 / ↓ 목록이 많을 경우 이렇게 확인함
변수명 = 변수명.index('찾을 item')
print(f'변수명: {변수명}')

enumerate = 리스트 목록에 있는 인덱스 번호가 확인됨
for idx, 변수명 in enumerate(변수명):
    print(idx , 변수명)

randint = 랜덤으로 숫자를 뽑아주는 함수
factoral = 팩토리얼 ex)4!은 1x2x3x4 = 24
item = 데이터
len = 리스트 길이 조회
list(리스트) = [] 이용해서 데이터를 묶고 , 데이터를 구분
continue = 아래 모두 무시한후 다시 처음부터 진행
break = 브레이크 걸리면 조건문 무조건 종료
while = 조건에 의한 반복
range = 범위를 지정하는 함수
for = 횟수에 의한 반복
pass = 넘김
% = 나머지
== = 같다
// = 몫
operator = 연산자​
import = 모듈 가져옴
elsr = 거짓일때 씀
if = 특정 조건에 의해서 프로그램 분기 / 만약에 같다 
! = not 연산자
| = shlit + \  or 연산자 | 숫자는 2진수로 바꾸기때문에 파이썬에선 안쓰는게 좋음
& = and 연산자
+ = 데이터와 데티ㄹ어 연결
, =공백
input (자동개행x) / print (자동개행o)
변수명 = 123
변수명 = 변수명 = var1의 데이터 복사
f (포멧스트링) , format= 심플 한 방법
0과 1로 순서를 정할 수 있다
\n = 강제 개행 (중복사용가능)
end = 자동 개행 막는코드
, = 데이터와 데이터 사이에 공백
\ = \ (무시라는 뜻도 있음)
숫자 = 정수열(int)
소수점 들어간 숫자 = 실수열 (float)
글자 = 문자열(bool , str)
true - flag = 논리형 / true 맞다 / 가능하다 / 실행해도된다
flag 아니다 / 멈춰라 / 실행 안 한다
input = 외부에서 데이터를 입력하는 코드 / 외부에서 입력하는 코드는 무조건 문자열로 들어감
자료형 변환 방법 ↑ 자료형 타입(변환시킬 데이터)

변경된 데이터로 쓸경우 변수명을 앞에 지정해줘야함 / 다시 메모리에 인식 시켜줘야함
str = string 다른 자료형을 문자열로 변환될때 str로 확인됨
type = 자료형을 강조할때 쓰임
print(type(변수명)) = 해당 데이터에 타입을 알려줌
''' = 주석과 같은 논리 / 범위를 넓게 이용 가능
# = 주석 > 읽지않는 무시하는 코드
import keyword
print(keyword.kwlist) = 사용한 변수명 확인
cd 파일명 = 체인지 디렉토리 (폴더이동)
dir = 열려있는 폴더 상태 확인
print = 화면에 출력하는 코드
"" / ''<- 문자열
'' > 빈문자 emoty character / ' ' >공백 문자
= = 할당연산자 / 개입연산자 우측의 데이터를 좌측의 변수에 "대입"하라는 뜻
컴파일러 방식 : 전체적으로 변경(저급언어) 후 실행
인터프리터 의 방식 : 한줄씩 할때마다 변경(저급언어) 후 실행
* = 에스타리크
변수 = 메모리에 데이터가 들어간 특정한 공간
변수명 = 메모리에 데이터가 들어간 특정한 공간의 이름
이름 = 데이터 -> 변수명 등록 (ex. num1 변수명 = 10 데이터)
"재사용할땐 변수이용 아닐시엔 이용x"
변수는 선언만 해서는 안됨 / 변수초기화를 해줘야함 공백문자도 넣을 수 있음
변수명 지을시 주의사항 , 대문자는 상수라는 의미(리펙토링 / 바꾸지말아야할 변수)영문자 및 소문자로 시작 , 데이터의 의미를 쉽게 확인할수있도록 짓는다
두개 이상의 단어가 조합될 경우 낙타표기법 따른다(ex newitme -> newItme ) 공백이 들어가는 자리 앞에 대문자를 넣으면 생각하기 쉽다 예약어는 변수명으로 사용x 언더바 제외한 특수문자 사용x 공백문자 x 숫자는 첫글자 제외 나머지 자리에서만 사용

